apiVersion: v1
kind: Service
metadata:
    name: "qubecluster-d3b5"
    labels:
        app: "qubecluster-d3b5"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "qubecluster-d3b5"