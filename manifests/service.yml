apiVersion: v1
kind: Service
metadata:
    name: "qubecluster-35aa"
    labels:
        app: "qubecluster-35aa"
spec:
    type: LoadBalancer
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "qubecluster-35aa"