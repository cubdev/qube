apiVersion: v1
kind: Service
metadata:
    name: "qubecluster-10e7"
    labels:
        app: "qubecluster-10e7"
spec:
    type: LoadBalancer
    ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
    selector:
        app: "qubecluster-10e7"